<h2>商品詳細</h2>

<%= image_tag @item.get_item_image(300,200) %>

<div class= "item_name">
  商品名
  <%= @item.name %>
</div>
<div>
  商品説明
  <%= @item.introduction %>
</div>
<div>
  ジャンル
  <%= @item.genre.name %>
</div>
<div>
  税込価格（税抜価格）
  <%= @item.add_tax_price %>
  （<%= @item.price %>）円
</div>
<div>
  販売ステータス
  <%= @item.is_active %>
</div>
<div>
<%= link_to "編集する", edit_admin_item_path(@item.id) %>
</div>