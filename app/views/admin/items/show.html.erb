<h2>商品詳細</h2>

<%= image_tag @item.image %>
<h4>商品名</h4>
  <%= @item.name %>
<h4>商品説明</h4>
  <%= @item.introduction %>
<h4>ジャンル</h4>
  <%= @item.genre.name %>
<h4>税込価格（税抜価格）</h4>
<%= @item.add_tax_price %>
（<%= @item.price %>）円

<h4>販売ステータス</h4>
<%= @item.is_active %>

<div>
<%= link_to "編集する", edit_admin_item_path(@item.id) %>
</div>