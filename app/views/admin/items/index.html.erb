<h2>商品一覧</h2>

<% @items.each do |item| %>

<h4>商品ID</h4>
  <%= item.genre_id %>
<h4>商品名</h4>
  <%= item.name %>
<h4>税抜価格</h4>
  <%= item.price %>
<h4>ジャンル</h4>
  <%= item.genre.name %>
<h4>ステータス</h4>
  <%= item.is_active %>
<% end %>